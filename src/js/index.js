import '../css/reset.scss';
import CanvasRibbons from './ribbons.js';

/* eslint-disable */ 
new CanvasRibbons({
    size: 90, // 彩带的宽度
    alpha: 0.6, // 透明度
    zIndex: -1 // z-index
});
/* eslint-enable */